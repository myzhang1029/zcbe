# Config file for a project
# Placed in $SRCROOT/zcbe/conf.yml
---
package-name: pkg-nm
package-version: pkg-ver
dependencies:
        build: # Build-time dependencies
                - bud1
                - bud2
        required: # Required packages
                - req1
                - req2
        recommended: # Recommended packages
                - rec1
                - rec2
        optional: # Optional packages
                - opt1
                - opt2

requirements:
        required: # Required when running
                - req3
                - req4
        recommended: # Recommended
                - rec3
                - rec4
        optional: # Optional
                - opt3
                - opt4
...
